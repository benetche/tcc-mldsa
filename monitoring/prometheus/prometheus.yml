global:
  scrape_interval: 10s
  evaluation_interval: 10s
  external_labels:
    monitor: iomt-lab

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # Host de laboratório (PC) — CPU, RAM, disco, rede
  - job_name: node-host
    static_configs:
      - targets: ['node-exporter:9100']
        labels:
          role: lab-host

  # Containers Docker (peers Fabric, orderer, chaincode CCAAS)
  - job_name: cadvisor
    static_configs:
      - targets: ['cadvisor:8080']
        labels:
          role: fabric-docker

  # Telegraf: ESP32 (MQTT) + Docker (containers Fabric/IoMT). Perfil mqtt.
  - job_name: telegraf
    static_configs:
      - targets: ['telegraf:9273']
        labels:
          role: iomt-telegraf

  # Raspberry Pi (node_exporter). Solte um JSON em targets/ (ver pi.example.json).
  - job_name: raspberry-pi
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/*.json
