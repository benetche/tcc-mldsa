# Cursor (agentes, roadmap, tasks — uso local; não publicar)
.cursor/

# Credenciais de laboratório (SSH, Wi-Fi, MQTT) — nunca versionar
edge/raspberry-pi/lab.env
edge/raspberry-pi/config.env
edge/esp32/main/secrets.h
edge/esp32/sdkconfig
.env.*
!.env.example
*.pem
*.key
wallet/
organizations/*/peerOrganizations/*/users/**/msp/keystore/*
!**/.gitkeep

# Monografia LaTeX (privada — não publicar no repositório remoto)
monografia/

# jq baixado localmente (ensure-jq.sh)
fabric/baseline/bin/jq

# Fabric gerado localmente
fabric/baseline/organizations/
fabric/baseline/channel-artifacts/
fabric/baseline/system-genesis-block/
fabric/baseline/logs/
fabric/mldsa/lab-msp/
fabric/mldsa/docker/dist/
fabric/mldsa/organizations/
fabric/mldsa/channel-artifacts/

# Go
vendor/
**/bin/
**/*.test

# Python
__pycache__/
.venv/
*.pyc

# liboqs (build local Pilar 2)
crypto/lib/
crypto/.liboqs-src/
crypto/.fabric-src/

# Build embarcado
edge/esp32/build/
edge/esp32/sdkconfig.old
.pio/

# Benchmarks (dados brutos)
benchmarks/results/*
!benchmarks/results/.gitkeep

# Benchmarks — artefatos derivados (regeráveis via analyze.py)
benchmarks/reports/summary.json
benchmarks/reports/matriz-resultados.md
benchmarks/reports/figures/

# Pilar 5 — dados de séries temporais (Prometheus/Grafana)
monitoring/data/
monitoring/grafana/data/
monitoring/.env
# Alvos reais do Prometheus (IPs do lab) — manter só o template *.json.example
monitoring/prometheus/targets/*.json

# MIMIC-IV (nunca versionar dados reais)
health-data/mimic/raw/
scripts/ingestion/.env
scripts/ingestion/.venv/
*.csv
*.parquet

# IDE / OS
.idea/
.vscode/
*.code-workspace
*.swp
.DS_Store

# Clone local opcional (Hyperledger samples)
fabric-samples/

# Logs
*.log
